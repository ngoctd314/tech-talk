package demo

import "github.com/fatih/color"

var cyanLog = color.New(color.FgCyan)
var cyanLogBold = cyanLog.Add(color.Bold)

var redLog = color.New(color.FgRed)
var redLogBold = redLog.Add(color.Bold)
